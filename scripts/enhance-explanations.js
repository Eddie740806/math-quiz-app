/**
 * 增強解析深度
 * 1. 修復 14 題短解析
 * 2. 增強 100 題幾何題解析
 */
const fs = require('fs');

// 讀取題庫
const geometryPath = './src/data/questions-geometry.json';
const mainPath = './src/data/questions.json';

const geometryData = require('.' + geometryPath);
const mainData = require('.' + mainPath);

// 幾何題解析模板
const geometryTemplates = {
  rectangle: (length, width, area) => `【解題步驟】
1. 辨識圖形：這是一個長方形
2. 找出條件：長 = ${length} 公分，寬 = ${width} 公分
3. 套用公式：長方形面積 = 長 × 寬
4. 代入計算：${length} × ${width} = ${area} 平方公分

💡 小提醒：長方形面積公式要記熟，長和寬相乘就是面積！`,

  square: (side, area) => `【解題步驟】
1. 辨識圖形：這是一個正方形
2. 找出條件：邊長 = ${side} 公分
3. 套用公式：正方形面積 = 邊長 × 邊長
4. 代入計算：${side} × ${side} = ${area} 平方公分

💡 小提醒：正方形四邊等長，面積就是邊長的平方！`,

  triangle: (base, height, area) => `【解題步驟】
1. 辨識圖形：這是一個三角形
2. 找出條件：底 = ${base} 公分，高 = ${height} 公分
3. 套用公式：三角形面積 = 底 × 高 ÷ 2
4. 代入計算：${base} × ${height} ÷ 2 = ${area} 平方公分

💡 小提醒：三角形面積是「底乘高除以2」，記住要除以2！
⚠️ 常見錯誤：忘記除以2，直接用底×高`,

  parallelogram: (base, height, area) => `【解題步驟】
1. 辨識圖形：這是一個平行四邊形
2. 找出條件：底 = ${base} 公分，高 = ${height} 公分
3. 套用公式：平行四邊形面積 = 底 × 高
4. 代入計算：${base} × ${height} = ${area} 平方公分

💡 小提醒：平行四邊形和長方形公式一樣，都是底×高！
⚠️ 注意：高是垂直於底的距離，不是斜邊長度`,

  trapezoid: (top, bottom, height, area) => `【解題步驟】
1. 辨識圖形：這是一個梯形
2. 找出條件：上底 = ${top} 公分，下底 = ${bottom} 公分，高 = ${height} 公分
3. 套用公式：梯形面積 = (上底 + 下底) × 高 ÷ 2
4. 代入計算：(${top} + ${bottom}) × ${height} ÷ 2 = ${top + bottom} × ${height} ÷ 2 = ${area} 平方公分

💡 口訣：「上底加下底的和，乘高除以2」
⚠️ 常見錯誤：忘記除以2，或搞混上底下底`,

  circle: (radius, area) => `【解題步驟】
1. 辨識圖形：這是一個圓形
2. 找出條件：半徑 r = ${radius} 公分
3. 套用公式：圓面積 = π × r²（π × 半徑 × 半徑）
4. 代入計算：3.14 × ${radius} × ${radius} = ${area} 平方公分

💡 公式記憶：圓面積要用「π r 平方」
⚠️ 常見錯誤：把半徑當直徑用，或忘記平方`,

  circumference: (radius, circumference) => `【解題步驟】
1. 辨識圖形：這是一個圓形，求周長
2. 找出條件：半徑 r = ${radius} 公分，直徑 = ${radius * 2} 公分
3. 套用公式：圓周長 = π × 直徑 = 2 × π × 半徑
4. 代入計算：3.14 × ${radius * 2} = ${circumference} 公分

💡 公式記憶：周長是「π × 直徑」或「2πr」
⚠️ 常見錯誤：混淆周長和面積的公式`,

  shadow: (side, squareArea, circleArea, shadowArea) => `【解題步驟】
1. 分析圖形：正方形內有一個內切圓，求陰影（正方形扣掉圓）
2. 找出條件：正方形邊長 = ${side} 公分，圓的直徑 = 邊長 = ${side} 公分
3. 計算正方形面積：${side} × ${side} = ${squareArea} 平方公分
4. 計算圓的半徑：${side} ÷ 2 = ${side/2} 公分
5. 計算圓面積：3.14 × ${side/2} × ${side/2} = ${circleArea} 平方公分
6. 計算陰影面積：正方形 - 圓 = ${squareArea} - ${circleArea} = ${shadowArea} 平方公分

💡 解題關鍵：內切圓的直徑 = 正方形邊長
⚠️ 常見錯誤：把邊長當成圓的半徑`,

  sector: (radius, angle, area) => `【解題步驟】
1. 辨識圖形：這是一個扇形（圓的一部分）
2. 找出條件：半徑 = ${radius} 公分，圓心角 = ${angle}°
3. 理解概念：扇形面積 = 整個圓面積 × (角度/360)
4. 套用公式：π × r² × (角度/360)
5. 代入計算：3.14 × ${radius} × ${radius} × (${angle}/360) = ${area} 平方公分

💡 理解方式：${angle}° 是整圓 360° 的 ${(angle/360*100).toFixed(1)}%，所以面積也是整圓的 ${(angle/360*100).toFixed(1)}%
⚠️ 常見錯誤：忘記乘以 (角度/360) 這個比例`
};

// 從解析中提取數字
function extractNumbers(explanation) {
  const matches = explanation.match(/\d+\.?\d*/g);
  return matches ? matches.map(Number) : [];
}

// 增強幾何題解析
console.log('🔧 增強幾何題解析...\n');

let enhanced = 0;
geometryData.questions.forEach(q => {
  const exp = q.explanation || '';
  const numbers = extractNumbers(exp);
  
  // 長方形
  if (exp.includes('長方形面積') && exp.includes('長 × 寬')) {
    const match = exp.match(/(\d+)\s*×\s*(\d+)\s*=\s*(\d+)/);
    if (match) {
      q.explanation = geometryTemplates.rectangle(parseInt(match[1]), parseInt(match[2]), parseInt(match[3]));
      enhanced++;
    }
  }
  // 正方形
  else if (exp.includes('正方形面積') && exp.includes('邊長 × 邊長')) {
    const match = exp.match(/(\d+)\s*×\s*\1\s*=\s*(\d+)/);
    if (match) {
      q.explanation = geometryTemplates.square(parseInt(match[1]), parseInt(match[2]));
      enhanced++;
    }
  }
  // 三角形
  else if (exp.includes('三角形面積') && exp.includes('÷ 2')) {
    const match = exp.match(/(\d+)\s*×\s*(\d+)\s*÷\s*2\s*=\s*(\d+)/);
    if (match) {
      q.explanation = geometryTemplates.triangle(parseInt(match[1]), parseInt(match[2]), parseInt(match[3]));
      enhanced++;
    }
  }
  // 平行四邊形
  else if (exp.includes('平行四邊形面積')) {
    const match = exp.match(/(\d+)\s*×\s*(\d+)\s*=\s*(\d+)/);
    if (match) {
      q.explanation = geometryTemplates.parallelogram(parseInt(match[1]), parseInt(match[2]), parseInt(match[3]));
      enhanced++;
    }
  }
  // 梯形
  else if (exp.includes('梯形面積')) {
    const match = exp.match(/\((\d+)\s*\+\s*(\d+)\)\s*×\s*(\d+)\s*÷\s*2\s*=\s*(\d+)/);
    if (match) {
      q.explanation = geometryTemplates.trapezoid(parseInt(match[1]), parseInt(match[2]), parseInt(match[3]), parseInt(match[4]));
      enhanced++;
    }
  }
  // 圓面積
  else if (exp.includes('圓面積') && exp.includes('π × r²')) {
    const match = exp.match(/3\.14\s*×\s*(\d+)\s*×\s*\1\s*=\s*([\d.]+)/);
    if (match) {
      q.explanation = geometryTemplates.circle(parseInt(match[1]), parseFloat(match[2]));
      enhanced++;
    }
  }
  // 圓周長
  else if (exp.includes('圓周長')) {
    const match = exp.match(/3\.14\s*×\s*(\d+)\s*=\s*([\d.]+)/);
    if (match) {
      const diameter = parseInt(match[1]);
      q.explanation = geometryTemplates.circumference(diameter / 2, parseFloat(match[2]));
      enhanced++;
    }
  }
  // 陰影面積
  else if (exp.includes('陰影面積')) {
    const sideMatch = q.content.match(/邊長\s*(\d+)/);
    if (sideMatch) {
      const side = parseInt(sideMatch[1]);
      const squareArea = side * side;
      const circleArea = Math.round(3.14 * (side/2) * (side/2) * 10) / 10;
      const shadowArea = Math.round((squareArea - circleArea) * 10) / 10;
      q.explanation = geometryTemplates.shadow(side, squareArea, circleArea, shadowArea);
      enhanced++;
    }
  }
  // 扇形
  else if (exp.includes('扇形面積')) {
    const radiusMatch = q.content.match(/半徑\s*(\d+)/);
    const angleMatch = q.content.match(/圓心角\s*(\d+)/);
    if (radiusMatch && angleMatch) {
      const radius = parseInt(radiusMatch[1]);
      const angle = parseInt(angleMatch[1]);
      const area = Math.round(3.14 * radius * radius * (angle / 360) * 10) / 10;
      q.explanation = geometryTemplates.sector(radius, angle, area);
      enhanced++;
    }
  }
});

console.log(`✅ 增強了 ${enhanced} 題幾何題解析\n`);

// 增強主題庫中的短解析
console.log('🔧 增強主題庫短解析...\n');

const shortExplanations = {
  'g5-da-207': `【解題步驟】
1. 理解題意：需要計算 4.5×2-1.2 的值
2. 先乘除後加減：先算 4.5×2 = 9
3. 再做減法：9 - 1.2 = 7.8

💡 運算順序：乘除優先，再做加減`,

  'g5-sp-630': `【解題步驟】
1. 理解題意：計算 1.2 的三次方
2. 連乘計算：1.2 × 1.2 = 1.44
3. 繼續乘：1.44 × 1.2 = 1.728

💡 次方意義：1.2³ 就是 1.2 連乘 3 次`,

  'g6-pc-101': `【解題步驟】
1. 理解題意：商品連續漲價兩次，每次漲 10%
2. 第一次漲價後：原價 × 1.1
3. 第二次漲價後：× 1.1 × 1.1 = × 1.21
4. 結論：總共漲了 21%

💡 關鍵概念：連續漲價要用乘法，不是直接加
⚠️ 常見錯誤：以為漲 10% 兩次就是漲 20%`,

  'g6-pc-104': `【解題步驟】
1. 理解題意：先漲 20%，再打 8 折
2. 漲 20% 後：原價 × 1.2
3. 打 8 折後：× 1.2 × 0.8 = × 0.96
4. 結論：最終是原價的 96%（降了 4%）

💡 關鍵概念：漲價用 1+漲幅，打折用折數
⚠️ 常見錯誤：以為漲 20% 再打 8 折會回到原價`,

  'g6-pc-106': `【解題步驟】
1. 理解題意：售價 500 元，利潤是成本的 25%
2. 設成本為 x，則：x × 1.25 = 500
3. 解方程：x = 500 ÷ 1.25 = 400
4. 答案：成本是 400 元

💡 利潤率公式：售價 = 成本 ×（1 + 利潤率）`,

  'g6-pc-107': `【解題步驟】
1. 理解題意：成本加成 40%，再打 75 折
2. 加成後：成本 × 1.4
3. 打折後：× 1.4 × 0.75 = × 1.05
4. 結論：最終利潤率是 5%

💡 實際利潤 = 1.05 - 1 = 0.05 = 5%`,

  'g6-pc-110': `【解題步驟】
1. 理解題意：某數增加 20% 後是 60
2. 設原數為 x，則：x × 1.2 = 60
3. 解方程：x = 60 ÷ 1.2 = 50
4. 答案：原數是 50

💡 增加 20% 就是乘以 1.2`,

  'g6-pf-202': `【解題步驟】
1. 理解題意：150 增加 40% 是多少
2. 增加 40%：150 × 1.4
3. 計算：150 × 1.4 = 210
4. 答案：210

💡 增加 40% = 原數 × (1 + 40%) = 原數 × 1.4`,

  'g6-pb-303': `【解題步驟】
1. 理解題意：先增加 25%，再增加 20%
2. 第一次增加後：× 1.25
3. 第二次增加後：× 1.25 × 1.2 = × 1.5
4. 結論：總共提高了 50%

💡 連續增加要用乘法！1.5 - 1 = 0.5 = 50%`,

  'g6-pf-503': `【解題步驟】
1. 理解題意：連續漲價三次，每次漲 10%
2. 計算：1.1 × 1.1 × 1.1 = 1.1³ = 1.331
3. 結論：總共漲了 33.1%

💡 連續漲價 n 次，結果是 (1+漲幅)ⁿ
⚠️ 常見錯誤：以為漲 10% 三次就是漲 30%`
};

let mainEnhanced = 0;
mainData.questions.forEach(q => {
  if (shortExplanations[q.id]) {
    q.explanation = shortExplanations[q.id];
    mainEnhanced++;
  }
});

console.log(`✅ 增強了 ${mainEnhanced} 題主題庫解析\n`);

// 保存
fs.writeFileSync(geometryPath, JSON.stringify(geometryData, null, 2), 'utf8');
fs.writeFileSync(mainPath, JSON.stringify(mainData, null, 2), 'utf8');

console.log('========================================');
console.log(`🎉 完成！共增強 ${enhanced + mainEnhanced} 題解析`);
console.log('========================================');
